package com.livetl.android.data.chat

import io.ktor.client.HttpClient
import kotlinx.serialization.json.Json

class ChatService(private val client: HttpClient, private val json: Json) {
    // TODO
}